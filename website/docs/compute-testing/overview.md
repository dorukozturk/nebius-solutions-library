---
sidebar_position: 1
---

# Compute Testing Overview

`compute-testing` is a lightweight Terraform/testing recipe in this repository with a small `terraform.tfvars` surface.

## What it appears to cover

From the files in the directory, this looks like a helper or validation-oriented setup rather than a fully documented end-user solution.

The visible inputs are focused on:

- project selection
- subnet selection
- SSH public key injection

## Configuration

The tfvars content from `compute-testing/terraform.tfvars` is documented here:

- [Generated configuration reference](./generated-configuration-reference.md)
